@model ShowApp.Models.Show

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Show</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShowTitle)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShowTitle)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShowGenre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShowGenre)
        </dd>
    </dl>
    <img src="@Model.ImagePath" alt="@Model.ShowTitle" />
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
